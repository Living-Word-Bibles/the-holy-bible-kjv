{
  "name": "the-holy-bible-kjv",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch:data": "node fetch-data.mjs",
    "build": "node build.mjs",
    "rebuild": "npm run fetch:data && npm run build"
  },
  "engines": { "node": ">=18" }
}
